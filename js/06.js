//Objeto Math
let resultado;

resultado = Math.PI; //numero PI
resultado = Math.round(2.5); //redondeo -4 hacia abajo +5 hacia arriba
resultado = Math.ceil(2.1); //redondeo hacia arriba
resultado = Math.floor(2.9); //redondeo hacia abajo, se puede implementar en un paginador
resultado = Math.sqrt(49);//raiz cuadrada
resultado = Math.abs(-7);//trasforma el numero en positivo
resultado = Math.min(8, 6, 9, 7, 8, 1, 3, 2);//toma el valor minimo de un listado
resultado = Math.max(8, 6, 9, 7, 8, 1, 3, 2);//toma el valor máximo de un listado
resultado = Math.random();//toma el valor aleatorio que no supera el 1 ej:0.1231
resultado = Math.floor(Math.random() * 30);//se puede utilizar un objeto y dentro otro objeto

console.log(resultado);


